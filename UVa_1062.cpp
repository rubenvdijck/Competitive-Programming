#include <bits/stdc++.h>
using namespace std;

int main(){
	string str;
	int TC = 1;
	while( getline(cin, str) && str != "end" ){
		char ch;
		stack<char> st;

		istringstream tmp(str);
		while( tmp >> ch ){
			st.push(ch);
		}


		while( !st.empty() ){

		} 



	}
	return 0;
}